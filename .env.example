# Base de datos SQLite (el archivo se persiste en un volumen, ver docker-compose.yml)
DATABASE_URL="file:/app/data/prod.db"

# NextAuth - generar con: openssl rand -base64 32
NEXTAUTH_SECRET="reemplazar-por-un-valor-aleatorio-largo"
NEXTAUTH_URL="https://tu-dominio.ejemplo.com"

# Cuenta inicial de gestión del sistema. Solo se usa para arrancar una base de
# datos vacía: si ya existe un usuario con ese DNI, no se crea nada. Podés
# dejar estas variables sin definir y el arranque las ignora.
ADMIN_DNI=""
ADMIN_PASSWORD=""
ADMIN_NOMBRE=""
ADMIN_APELLIDO=""
